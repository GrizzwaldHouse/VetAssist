{
  "name": "@vetassist/shared-utils",
  "version": "0.1.0",
  "description": "VetAssist shared utility functions — pure functions, no side effects",
  "author": "Marcus Daley",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:pii": "vitest run pii --passWithNoTests"
  },
  "dependencies": {
    "@vetassist/shared-config": "*",
    "@vetassist/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
