{
  "name": "@vetassist/legal",
  "version": "0.1.0",
  "description": "VetAssist legal boundary engine — detects and rewrites claim advice, eligibility statements, directive language",
  "author": "Marcus Daley",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:compliance": "vitest run --reporter=verbose --passWithNoTests"
  },
  "dependencies": {
    "@vetassist/events": "*",
    "@vetassist/shared-config": "*",
    "@vetassist/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
