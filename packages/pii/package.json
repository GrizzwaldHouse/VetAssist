{
  "name": "@vetassist/pii",
  "version": "0.1.0",
  "description": "VetAssist PII detection engine — detects and blocks SSN, credit card, VA file numbers",
  "author": "Marcus Daley",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:pii": "vitest run --reporter=verbose --passWithNoTests"
  },
  "dependencies": {
    "@vetassist/events": "*",
    "@vetassist/shared-config": "*",
    "@vetassist/shared-types": "*",
    "@vetassist/shared-utils": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
